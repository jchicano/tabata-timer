import styled from 'styled-components'

export const Selectors = styled.div`
    width: 900px;
    max-width: 100%;
    margin: 0 auto;

    display: grid;
    grid-template-columns: 1fr;
    gap: 1rem;
`
